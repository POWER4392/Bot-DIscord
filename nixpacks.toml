[providers]
node = "20"

[phases.setup]
nixPkgs = ["python311", "ffmpeg", "nodejs_20", "libopus", "libsodium"]
aptPkgs = ["ffmpeg", "libopus-dev", "libsodium-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]
